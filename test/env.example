# 環境變數示例文件
# 複製此文件並根據實際情況填入值

# ============================================
# Railway 後端環境變數
# ============================================
SPRING_DATASOURCE_URL=jdbc:mysql://containers-us-west-xxx.railway.app:3306/railway?useSSL=true&serverTimezone=Asia/Hong_Kong
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=your_mysql_password_here
SECURITY_JWT_SECRET=your-super-secret-jwt-key-must-be-at-least-32-characters-long
SECURITY_JWT_EXPIRATION_MINUTES=60
CORS_ALLOWED_ORIGINS=https://your-app-name.vercel.app

# ============================================
# Vercel 前端環境變數
# ============================================
VITE_API_BASE_URL=https://your-backend-name.railway.app/api

# ============================================
# 生成 JWT Secret 的方法（在終端執行）：
# ============================================
# Windows PowerShell:
# -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
#
# Linux/Mac:
# openssl rand -base64 32
#
# 或在線生成器：
# https://www.grc.com/passwords.htm


